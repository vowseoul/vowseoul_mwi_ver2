# 의존성 패키지 (터미널에서 npm install 등으로 다시 설치 가능하므로 업로드 제외)
node_modules/

# 빌드 결과물 (배포용 파일)
dist/
build/

# 운영체제 시스템 파일
.DS_Store
Thumbs.db

# 환경변수 및 비밀키 파일
.env

# .gitignore 파일 내용에 아래 줄이 반드시 있어야 합니다
.next/
node_modules/
.env*.local

# TypeScript 증분 빌드 캐시
tsconfig.tsbuildinfo