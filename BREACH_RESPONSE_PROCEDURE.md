# 개인정보 유출 대응 절차

> 작성일: 2026-08-12 · 근거 법령: 개인정보 보호법 제34조, 동법 시행령 제39조
>
> **이 문서는 실제 사고가 터진 뒤 펼쳐 보는 문서다.** 평소엔 안 읽힌다는 전제로,
> 맨 위에 "지금 뭘 눌러야 하는지"부터 적는다.

---

## 0. 지금 유출 정황을 발견했다면 (5분 안에 할 일)

1. **더 새는 걸 막는다** — 아래 §2 "즉시 차단"부터 실행
2. **§4의 CPO 연락처로 즉시 알린다** (설정 > 보안에 등록된 값 — [app/admin/(dashboard)/settings/page.tsx](<app/admin/(dashboard)/settings/page.tsx>) 보안 탭)
3. **시각을 기록한다** — 언제 발견했는지가 72시간 기산점이다 (§1)
4. 증거가 될 로그·화면을 **삭제하지 말고 캡처**해둔다 (원인 조사·재발방지에 필요)

---

## 1. 법정 기한 — 72시간

개인정보 보호법 제34조: 유출 사실을 **알게 된 때로부터 72시간 이내**에

- 정보주체(피해자)에게 개별 통지
- 1천 명 이상 유출 시 개인정보보호위원회 또는 한국인터넷진흥원(KISA)에 신고 (**의무**)
- 1천 명 미만이어도 **신고를 권장** — 나중에 규모가 더 컸던 것으로 밝혀지면 지연 신고로 별도 문제가 된다

72시간을 넘기면 유출 자체와 별개로 **지연 통지·신고**가 추가 과태료 사유가 된다. "원인을 다 밝히고 나서 통지하겠다"는 늦다 — 안 것만으로 시계가 돈다. 정황만 파악되면 1차 통지부터 나가고, 세부 사항은 추가 통지로 보완한다.

---

## 2. 이 서비스 기준 즉시 차단 방법

이 프로젝트(Supabase + Vercel)에서 실제로 존재하는 유출 경로별 차단 방법이다.

| 유출 경로 | 즉시 조치 |
|---|---|
| **service_role 키 노출** (코드 커밋, 로그 노출 등) | Supabase 대시보드 > Project Settings > API에서 키 **즉시 재발급(rotate)**. 재발급 즉시 `SUPABASE_SERVICE_ROLE_KEY` 환경변수(Vercel)를 새 값으로 교체하고 재배포 — 구 키는 재발급 순간 무효화된다 |
| **DASHBOARD_SESSION_SECRET 노출** | 재발급 즉시 발급된 모든 신랑신부 대시보드·검수 세션 쿠키가 무효화된다(서명 불일치) — 재로그인만 필요하고 데이터 유출 자체는 막힌다. 환경변수 교체 후 재배포 |
| **관리자 계정 탈취 의심** | Supabase Auth 대시보드에서 해당 계정 세션 강제 만료 + 비밀번호 초기화. `profiles` 테이블에서 역할·권한 확인 |
| **DB 자체 침해 의심** (RLS 우회, SQL 인젝션 등) | Supabase 대시보드에서 anon/authenticated 키도 재발급 검토. `audit_logs`·Supabase 대시보드의 API 로그에서 비정상 조회 패턴 확인 |
| **Storage 버킷 무단 접근** | `vow-seoul-storage` 버킷은 공개 읽기 정책이라(§[supabase/migrations/20260709000000_storage_bucket.sql](supabase/migrations/20260709000000_storage_bucket.sql)) 이 자체는 "유출"이 아니라 설계다. 다만 URL이 추측 불가능한 uuid 파일명이라는 전제가 깨진 경우(예: 목록 API가 열려 있었다면) 해당 정책부터 재검토 |

---

## 3. 영향 범위 파악 — 무엇을 확인해야 하는가

1. **어떤 데이터가 새어나갔는가** — `PRIVACY_COMPLIANCE_PLAN.md` §1에 정리된 수집 지점 기준으로 판단한다:
   - 신랑신부(고객) 정보: `customers`, `invitations.content_data` (성명·연락처·계좌정보 포함)
   - 하객 정보: `rsvp_responses`, `guestbook_entries`
   - 접속기록: `visit_logs`, `audit_logs`
2. **몇 명이 영향받았는가** — 해당 테이블에서 유출 시점 기준 행 수를 센다. 1천 명 기준으로 신고 의무 여부가 갈린다(§1)
3. **언제부터 언제까지인가** — `created_at`/`updated_at` 컬럼과, 원인이 키 노출이라면 그 키가 커밋되거나 로그에 찍힌 시점을 기준으로 범위를 좁힌다
4. **`audit_logs` 조회** — 관리자가 아닌 접근이 있었는지 확인 (§5 참고: 이 테이블 자체가 취급자 접속기록이다)

---

## 4. 통지·신고

### 4.1 정보주체 통지 — 포함해야 할 5가지 (법정 사항)

1. 유출된 개인정보 항목
2. 유출된 시점과 경위
3. 피해 최소화를 위해 정보주체가 할 수 있는 방법 (예: 계좌 유출 시 은행 신고 안내)
4. 회사의 대응조치 및 피해 구제절차
5. 담당부서 및 연락처 (§[app/privacy/page.tsx](app/privacy/page.tsx)에 이미 게시된 CPO 정보 활용)

**통지 방법**: 등록된 이메일 또는 연락처로 개별 통지가 원칙. 1천 명 이상이고 개별 통지가 과도한 비용을 요구하는 경우에 한해 홈페이지 게시로 갈음 가능(요건이 까다로우니 원칙은 개별 통지).

### 4.2 규제기관 신고

| 기관 | 연락처 | 신고 요건 |
|---|---|---|
| 개인정보보호위원회 | privacy.go.kr, 국번없이 182 | 1천 명 이상 유출 시 의무 |
| 한국인터넷진흥원(KISA) 개인정보침해신고센터 | privacy.kisa.or.kr, 국번없이 118 | 위와 동일, 위원회와 창구 일원화 |

신고서에는 §4.1의 5가지 항목 + 신고 경위 + 조치 현황을 포함한다.

---

## 5. 사후 조치

- [ ] 원인 조사 결과를 문서로 남긴다 (재발방지 대책의 근거)
- [ ] `audit_logs`에 이번 사고 대응 자체도 기록한다 (`action: "incident.responded"`)
- [ ] 재발방지 대책을 실행하고, 처리방침 §14(고지의무)에 따라 변경사항이 있으면 시행 7일 전 공지
- [ ] 유사 사고 방지를 위해 이 문서를 갱신한다

---

## 6. 역할 분담

| 역할 | 담당 | 하는 일 |
|---|---|---|
| 총괄 | 개인정보 보호책임자(CPO) | 통지·신고 판단, 대외 커뮤니케이션 |
| 기술 대응 | 개발 담당자 | §2 차단 조치, §3 영향범위 조사 |
| 신고 접수 | CPO 또는 위임자 | §4.2 규제기관 신고 |

소규모 조직에서는 위 역할을 한 사람이 겸임할 수 있다 — 다만 "누가 최종 판단을 내리는지"만은 사고 전에 미리 정해둬야 한다(사고 중에 정하면 늦다).

---

## 7. 이 문서가 다루지 않는 것

- 침입 탐지 시스템(IDS) 등 사전 탐지 인프라 구축 — 이 서비스 규모에서는 Supabase/Vercel 대시보드의 기본 로그로 대응한다. 트래픽이 커지면 재검토
- 법률 자문 — 실제 유출 발생 시 신고 문구·통지 대상 확정은 개인정보 전문 법률 자문을 받는 것을 권장한다. 이 문서는 "무엇을 언제까지 해야 하는지"의 체크리스트이지 법률 자문을 대체하지 않는다
