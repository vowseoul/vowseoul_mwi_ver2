# VOW SEOUL 개인정보보호법 대응 계획서

> 작성일: 2026-08-11 · 기준 브랜치: `beta` (`f86bd9e`)
> 근거 법령: 개인정보 보호법(2023.9.15 시행 개정법), 동법 시행령,
> 「개인정보의 안전성 확보조치 기준」(개인정보보호위원회 고시)
>
> **이 문서의 모든 "현재 상태"는 코드 실측 결과다.** 추정으로 쓴 항목은 §7에 따로 모았다.

---

## 1. 한 줄 진단

**수집은 이미 광범위하게 하고 있는데, 법이 요구하는 4가지 축(동의·고지·안전조치·파기)이 전부 비어 있다.**

이 서비스는 세 종류의 정보주체에게서 개인정보를 받는다.

| 정보주체 | 받는 정보 | 동의 | 고지 |
|---|---|---|---|
| 신랑신부 | 성명·연락처·예식정보·사진·**계좌번호** | ✗ | ✗ |
| 하객(응답자) | 성명·**전화번호**·참석정보 / 방명록 이름·메시지 | ✗ | ✗ |
| 하객(방문자 전원) | **IP·기기정보·유입경로** — 링크만 열어도 수집 | ✗ | ✗ |

특히 세 번째가 위험하다. 청첩장 링크를 연 사람은 **자신이 무언가 수집당했다는 사실 자체를 알 수 없다.**
청첩장은 카톡으로 무차별 전파되는 물건이라 정보주체 수가 빠르게 늘고, 그중 한 명만 신고해도
조사 대상이 된다.

> **가장 시급한 것 하나만 고른다면 → §6.1 개인정보 처리방침 게시.**
> 없는 상태 자체가 곧 위반이고(제30조), 다른 모든 항목의 고지 근거가 여기서 나온다.

---

## 2. 법 조문 대비 격차 지도

### 2.1 즉시 위반 상태 (P0)

| 조문 | 요구사항 | 현재 상태 (실측) | 위험 |
|---|---|---|---|
| **제30조** | 개인정보 처리방침 수립·공개 | `app/privacy/` **디렉터리 자체가 없음**. [components/footer.tsx:48](components/footer.tsx)이 `/privacy`를 링크하지만 404. 게다가 그 footer가 붙은 랜딩은 [app/page.tsx](app/page.tsx)에서 `/admin`으로 리다이렉트돼 **도달조차 불가** | 과태료 1천만원 이하 |
| **제15조·제22조** | 수집·이용 동의 (목적·항목·보유기간·거부권리 4종 고지 + 필수/선택 구분) | 4개 수집 지점 **전부 동의 절차 0건** — 폼([app/form/[slug]/page.tsx](app/form/%5Bslug%5D/page.tsx)), RSVP·방명록([components/invitation/slot-registry.tsx](components/invitation/slot-registry.tsx)), 문의([app/contact/page.tsx:33](app/contact/page.tsx)) | 과태료 5천만원 이하 |
| **제29조 + 고시 제7조** | 비밀번호는 **일방향 암호화** 저장 | `invitations.dashboard_password` 평문([initial_schema.sql:203](supabase/migrations/20260708000000_initial_schema.sql)). `form_instances.access_password` 평문이며 **브라우저로 전송된 뒤 클라이언트에서 문자열 비교**([useForms.ts:391 `select('*')`](hooks/queries/useForms.ts) → [form/[slug]/page.tsx:232](app/form/%5Bslug%5D/page.tsx)) | 안전조치 위반 |
| **제21조** | 보유기간 경과 시 **지체 없이 복구 불가능하게 파기** | ①고객 정보는 어떤 경로로도 하드 삭제 안 됨([useCustomers.ts:152](hooks/queries/useCustomers.ts) — `deleted_at`만) ②청첩장 자동 파기도 소프트 삭제([purge-expired-invitations/route.ts:77](app/api/cron/purge-expired-invitations/route.ts)) ③업로드 이미지는 Storage에 영구 잔존(`deleteFile()` 호출처가 폰트 삭제 1곳뿐) | 과태료 3천만원 이하 |

### 2.2 실효성 결함 (P0~P1)

| 조문 | 문제 | 근거 |
|---|---|---|
| 제21조 | **14일 하객정보 파기가 "신랑신부의 대시보드 접속"에 의존한다.** 크론이 아니라 페이지 방문이 방아쇠라, 예식 후 아무도 안 들어가면 하객 실명·전화번호가 무기한 잔존 | [dashboard/page.tsx:62-64](app/invitation/%5Bid%5D/dashboard/page.tsx) — `after(() => purgeCollectedData(id))`. [vercel.json](vercel.json)의 크론 2개 중 하객 데이터를 지우는 것은 없음 |
| 제21조 | 파기 루프가 `visit_daily_stats`(집계본)를 **빠뜨림** — 원본만 지우고 집계는 남김 | [dashboard/page.tsx:172](app/invitation/%5Bid%5D/dashboard/page.tsx) — 대상 3개 테이블에 미포함 |
| 제29조 | **IP 해시가 사실상 가역적이다.** 솔트 없는 `SHA-256(IPv4)`는 주소공간이 2³²라 전수 대입으로 수 초 내 원본 복원 가능 → 가명처리 미달, 개인정보 그대로 | [app/w/[slug]/page.tsx:174](app/w/%5Bslug%5D/page.tsx) — `createHash("sha256").update(ip)` |
| 제36조 | 정보주체(하객)가 **자기 정보를 지울 방법이 없다.** 방명록은 `password_hash` 컬럼이 있는데도 빈 문자열만 넣어 본인확인이 원천 불가 | [slot-registry.tsx:1012](components/invitation/slot-registry.tsx) 주석에 "실질적으로 쓰이지 않는 자리표시자"로 명시 |

### 2.3 고지 누락 (P1)

| 조문 | 요구사항 | 현재 상태 |
|---|---|---|
| 제26조 | **처리위탁** 시 수탁자·위탁업무를 처리방침에 공개 | Supabase(저장), Vercel(호스팅·Analytics), 네이버클라우드(지오코딩) 모두 수탁자에 해당하나 공개처 없음 |
| **제28조의8** | **국외이전** 시 이전항목·국가·수탁자·목적·보유기간을 **동의받거나 처리방침에 공개** | Supabase·Vercel 리전이 해외면 전부 국외이전. 현재 어느 쪽도 없음 → §7에서 리전 확인 필요 |
| 제30조 제1항 7호 | 자동수집장치(쿠키·접속기록) 설치·운영 및 거부 방법 | Vercel Analytics([app/layout.tsx:71](app/layout.tsx))와 Google Fonts CDN([components/font-loader.tsx:26](components/font-loader.tsx))가 **루트 레이아웃에 있어 관리자·하객 전 페이지에서 매 로드마다** 외부로 IP 전송. 고지·거부수단 없음 |
| 제31조 | 개인정보 보호책임자(CPO) 지정·공개 | 지정 사실 없음 |

### 2.4 후속 정비 (P2)

| 조문 | 요구사항 | 현재 상태 |
|---|---|---|
| 고시 제8조 | 개인정보취급자 **접속기록 1년 이상 보관** | `audit_logs`는 있으나 *편집 이력* 전용. "관리자 누가 언제 하객 명단을 조회·다운로드했는가"는 미기록 |
| 제34조 | 유출 시 72시간 내 통지·신고 절차 | 문서·절차 없음 |
| 제22조의2 | 만 14세 미만 아동 동의 | 방명록·RSVP에 연령 확인 없음 (청첩장 특성상 저위험이나 안내 문구 권장) |

---

## 3. UI/UX 설계 원칙

법을 지키면서 **청첩장의 감성과 폼 완주율을 죽이지 않는 것**이 이 작업의 진짜 난이도다.
아래 5개 원칙을 모든 화면에 일관 적용한다.

### 원칙 ① 동의는 "관문"이 아니라 "제출 직전 한 줄"로

하객은 결혼식 초대를 받은 상태다. 법률 텍스트로 시작하면 이탈한다.
**수집이 실제로 일어나는 시점(제출 버튼)에 최소 마찰로 배치**한다.

- 기본 상태는 **접힘**. 체크박스 한 줄 + `⌄` 토글.
- 단, 체크박스 라벨에 `(필수)`를 반드시 노출 — 접혀 있어도 무엇에 동의하는지 보이게.
- 미체크 시 제출 버튼 `disabled`. 누르면 기존 `error` state 재사용해 안내.

> ⚠️ **폼(신랑신부)만은 예외로 "첫 진입 시" 동의를 받는다.**
> 현재 임시저장([form/[slug]/page.tsx:423](app/form/%5Bslug%5D/page.tsx))이 서버 `form_submissions`에
> INSERT 하므로, 최종 제출 전에 이미 수집이 시작된다. 최종 확인 스텝에 두면 **동의 없이 수집한 구간**이 생긴다.

### 원칙 ② 글씨 크기 하한선을 코드 규약으로 못박는다

시행령 제17조는 중요 내용을 "명확히 표시"하도록 요구하며, 실무 기준은 **9pt(≈12px) 이상**이다.
현행 아일랜드 코드에는 `fontSize: 10`, `11.5` 같은 값이 실제로 쓰이고 있다
([slot-registry.tsx:876](components/invitation/slot-registry.tsx) 등).

→ **동의 관련 텍스트에는 12px 미만을 쓰지 않는다.** 처리방침 *링크*는 예외(고지문이 아니므로).

### 원칙 ③ 하객 UI는 iframe 안이라 shadcn을 쓸 수 없다

RSVP·방명록은 테마 iframe 내부에 렌더되는 아일랜드다. 관리자 화면의 Tailwind/shadcn이
닿지 않으므로 **inline style + `accent` 토큰**이라는 기존 규약을 그대로 따라야 한다.
동의 UI를 shadcn `Checkbox`로 만들면 스타일이 통째로 깨진다.

→ 하객용과 관리자용 동의 컴포넌트를 **분리해서 2개** 만든다. (중복이지만 렌더 환경이 다르다)

### 원칙 ④ 처리방침 링크는 "모든 테마에 일괄" 주입한다

테마 HTML 템플릿마다 링크를 심으면 새 테마가 추가될 때마다 누락된다.
→ 테마 파일이 아니라 **`InvitationFrame`이 iframe 문서 최하단에 주입**한다. 테마 계약을 건드리지 않는다.

디자인은 최대한 절제 — 중앙정렬, `opacity 0.35`, 10px, 하단 여백 안쪽.

### 원칙 ⑤ 관리자 설정은 새 탭을 만들지 않고 기존 카드 옆에 붙인다

설정 화면에는 이미 `일반` 탭에 **"데이터 자동 파기"** 카드가 있다([settings/page.tsx:524](app/admin/%28dashboard%29/settings/page.tsx)).
개인정보 관련 설정은 성격이 같으므로 **같은 탭에 카드로 인접 배치**한다.
탭을 새로 파면(현재 7개) 모바일에서 가로 스크롤이 더 심해진다.

---

## 4. 화면별 설계 (텍스트 목업)

### 4.1 하객 — RSVP 모달

현재 구조 마지막에 동의 블록을 삽입하고 제출 버튼을 게이팅한다.

```
┌─ 참석 여부 전달 ──────────────────┐
│ 참석 여부와 인원을 알려주세요           │
│                                      │
│  성함      [                    ]     │
│  연락처    [ 010-0000-0000      ]     │
│  참석 여부  [ 참석 ] [ 불참 ]          │
│  참여 구분  [신랑측] [신부측]          │
│  참석 인원  [ − ]  2  [ ＋ ]          │
│  식사 여부  [한식][양식][안함]         │
│                                      │
│ ┌──────────────────────────┐  │
│ │ ☐ (필수) 개인정보 수집·이용 동의  ⌄│  │  ← 12px, accent 테두리
│ └──────────────────────────┘  │
│                                      │
│   [  취소  ]   [    전달하기    ]     │  ← 미체크 시 disabled
└──────────────────────────────┘

⌄ 펼친 상태 (12px, line-height 1.6)
┌──────────────────────────────┐
│ 목적    예식 참석 인원·식사 준비        │
│ 항목    성함, 연락처, 참석 정보        │
│ 보유    예식일로부터 14일 후 파기       │
│                                      │
│ 동의를 거부하실 수 있으며, 거부 시      │
│ 참석 회신 기능을 이용하실 수 없습니다.   │
│                                      │
│ 개인정보처리방침 전문 보기 →           │
└──────────────────────────────┘
```

**수정 파일:** [components/invitation/slot-registry.tsx](components/invitation/slot-registry.tsx) `RsvpIsland`
(제출 검증 `submit()`에 동의 체크 추가, `upsert_rsvp_response` RPC 인자에 동의 시각·버전 추가)

### 4.2 하객 — 방명록

동의 + **본인 삭제용 비밀번호**를 함께 도입한다. `password_hash` 컬럼이 이미 있으므로 스키마 변경 없이
"쓰이지 않던 자리표시자"를 실제로 쓰게 만드는 것이다 → 제36조(삭제 요구권) 대응이 여기서 해결된다.

```
┌──────────────────────────────┐
│  이름     [              ]            │
│  메시지   [                       ]   │
│  비밀번호 [ ● ● ● ● ]  ← 신규          │
│           내가 쓴 글을 지울 때 필요해요   │
│                                      │
│  ☐ (필수) 개인정보 수집·이용 동의  ⌄     │
│                     [   남기기   ]    │
└──────────────────────────────┘

목록의 각 항목 우측에 [삭제] → 비밀번호 확인 모달
```

### 4.3 하객 — 청첩장 최하단 (모든 테마 공통)

```
              ⋯ 청첩장 본문 끝 ⋯


                개인정보처리방침
```
10px / `opacity: .35` / 중앙정렬. **`InvitationFrame`이 주입**하므로 테마 HTML 무수정.

### 4.4 신랑신부 — 폼 첫 진입 (신규 스텝 0)

비밀번호 게이트 통과 직후, 기존 스텝 위저드 **앞에** 삽입한다.

```
┌──────────────────────────────┐
│           [ VOW SEOUL ]              │
│                                      │
│        정보 수집 안내                  │
│  청첩장 제작을 위해 아래 정보를 수집합니다 │
│                                      │
│ ┌─ 필수 ─────────────────────┐ │
│ │ 목적  청첩장 제작·발행 및 고객 응대 │ │
│ │ 항목  성함, 연락처, 예식 정보,     │ │
│ │      인사말, 사진, 계좌 정보      │ │
│ │ 보유  예식일 + 30일 후 파기        │ │
│ └────────────────────────────┘ │
│                                      │
│ 동의를 거부하실 수 있으나, 거부 시       │
│ 청첩장 제작이 불가능합니다.             │
│                                      │
│ ☐ 위 내용에 동의합니다 (필수)          │
│                                      │
│ 개인정보처리방침 전문 →                │
│                                      │
│         [  동의하고 시작하기  ]        │
└──────────────────────────────┘
```

- 보유기간 "예식일 + 30일"은 **하드코딩하지 않는다.** 관리자 설정값
  ([lib/data-retention.ts](lib/data-retention.ts) `DATA_RETENTION_SETTINGS_KEY`)을 읽어 렌더 →
  설정을 바꿨는데 고지문이 옛날 값이면 그 자체가 허위 고지가 된다.
- 계좌 정보를 수집 항목에 **반드시 명시**한다. 지금 사용자가 가장 예상 못 하는 항목이다.

### 4.5 문의하기

`app/contact/page.tsx`는 이름·이메일을 받으면서 동의가 없다. 동일 패턴의 체크박스 1개를
제출 버튼 위에 추가. (이 화면은 shadcn 환경이므로 관리자용 컴포넌트 재사용)

### 4.6 관리자 — 설정 > 일반 탭

기존 "데이터 자동 파기" 카드 **아래**에 카드 2개를 잇는다.

```
┌ 데이터 자동 파기 ─────────────┐  ← 기존
│ 예식일로부터  [ 30 ] 일 후 삭제   │
└──────────────────────────┘

┌ 개인정보 보호책임자 ───────────┐  ← 신규
│ 성명   [              ]         │
│ 직책   [              ]         │
│ 연락처 [              ]         │
│ 이메일 [              ]         │
│ ⓘ 처리방침에 공개되는 정보입니다   │
└──────────────────────────┘

┌ 개인정보 처리방침 ────────────┐  ← 신규
│ 현재 버전   v1.0                 │
│ 시행일     2026-08-20            │
│ [ 처리방침 페이지 열기 → ]        │
│ ⓘ 내용 변경 시 시행 7일 전 공지    │
└──────────────────────────┘
```

> **처리방침 본문은 설정에서 편집하지 않는다.** 리치텍스트 에디터를 붙이는 건 과잉이고,
> 법정 기재사항 14개를 관리자가 실수로 지울 위험이 크다. 본문은 코드(정적 페이지)로 관리하고,
> **사업자 정보·CPO·시행일 같은 변수만** 설정에서 주입한다.

---

## 5. 우선순위

```
P0 (배포 전 필수 · 없으면 위반 상태 지속)
   ① 개인정보 처리방침 페이지 + 도달 경로
   ② 4개 지점 수집 동의 (폼 · RSVP · 방명록 · 문의)
   ③ 비밀번호 평문 저장 해소

P1 (2~3주 내)
   ④ 파기 실효화 (크론 이관 + Storage + 하드 삭제)
   ⑤ IP 해시 강화
   ⑥ 하객 자기정보 삭제 수단 (방명록 비밀번호)

P2 (분기 내)
   ⑦ 취급자 접속기록 보관
   ⑧ 유출 대응 절차 문서화
```

---

## 6. 상세 구현 계획

### 6.1 [P0] 개인정보 처리방침 게시 — 제30조

**신규 파일**
- `app/privacy/page.tsx` — 정적 서버 컴포넌트. 법정 기재사항 14개 항목 구성
- `app/terms/page.tsx` — 이용약관. 법적 필수는 아니나 footer가 이미 링크하므로 404 방치는 부적절
- `lib/privacy-policy.ts` — 사업자명·주소·CPO·시행일·버전 상수 (본문과 변수 분리)

**도달 경로 확보 (현재 랜딩이 리다이렉트라 footer 링크가 무용지물)**
| 위치 | 방법 |
|---|---|
| 청첩장(하객) | `InvitationFrame`이 iframe 최하단 주입 — §4.3 |
| 폼(신랑신부) | 동의 스텝 + 폼 하단 링크 |
| 대시보드/셀프편집 | 페이지 하단 링크 |
| 관리자 | 설정 카드에서 열기 |

> `app/page.tsx`의 `redirect('/admin')`은 **그대로 둔다.** `/privacy`는 독립 라우트라
> 리다이렉트와 무관하게 직접 접근 가능하고, 랜딩 복구는 로드맵 §9 보류 항목이다.

**공수: 2~3일** (본문 작성이 대부분. §7 확인 정보 확보가 선행)

### 6.2 [P0] 수집 동의 — 제15조·제22조

**신규 파일**
- `lib/privacy-consent.ts` — 동의 항목 정의(목적·항목·보유기간·거부 시 불이익)와 `CONSENT_VERSION` 상수.
  보유기간은 `data-retention` 설정을 주입받는 함수형으로.
- `components/privacy-consent-field.tsx` — **관리자/폼용** (shadcn Checkbox 기반)
- `components/invitation/consent-notice.tsx` — **하객 아일랜드용** (inline style + accent, 원칙 ③)

**동의 사실 입증 (제15조 입증책임은 처리자에게 있다)**

별도 `consent_records` 테이블은 만들지 않는다. 수집 대상 행에 컬럼 2개를 붙이는 쪽이 단순하고,
행이 파기되면 동의기록도 함께 사라져 보관 정합성이 맞는다.

```sql
-- 신규 마이그레이션
ALTER TABLE public.rsvp_responses    ADD COLUMN consent_agreed_at timestamptz,
                                     ADD COLUMN consent_version text;
ALTER TABLE public.guestbook_entries ADD COLUMN consent_agreed_at timestamptz,
                                     ADD COLUMN consent_version text;
ALTER TABLE public.form_submissions  ADD COLUMN consent_agreed_at timestamptz,
                                     ADD COLUMN consent_version text;
ALTER TABLE public.inquiries         ADD COLUMN consent_agreed_at timestamptz,
                                     ADD COLUMN consent_version text;
```

`rsvp_responses`는 클라이언트가 직접 INSERT하지 않고 `upsert_rsvp_response` RPC를 거치므로
**RPC 시그니처도 함께 수정**해야 한다([slot-registry.tsx:835](components/invitation/slot-registry.tsx)).

**수정 파일**
| 파일 | 변경 |
|---|---|
| [app/form/[slug]/page.tsx](app/form/%5Bslug%5D/page.tsx) | 스텝 0 삽입, `handleSaveDraft`·`handleSubmit`에 동의 검증 |
| [components/invitation/slot-registry.tsx](components/invitation/slot-registry.tsx) | `RsvpIsland`·`GuestbookIsland`에 동의 블록 + 제출 게이팅 |
| [app/contact/page.tsx](app/contact/page.tsx) | 동의 체크박스 |

**공수: 4~5일**

### 6.3 [P0] 비밀번호 평문 저장 해소 — 제29조

두 비밀번호는 성격이 달라 **처리 방식을 나눈다.**

**(a) `form_instances.access_password` — 즉시 수정 필요**
지금은 `select('*')`로 브라우저까지 내려와 클라이언트에서 비교한다. 네트워크 탭에서 그대로 보인다.
→ 이미 검증된 `lib/dashboard-session.ts` 패턴을 재사용해 **서버 라우트로 이관**한다.

- `app/api/form-auth/route.ts` 신규 — `passwordMatches()` 재사용, 통과 시 서명 쿠키 발급
- `useFormInstanceBySlugQuery`의 `select('*')` → **명시적 컬럼 목록**으로 좁혀 비밀번호 제외

**(b) `invitations.dashboard_password` — 트레이드오프 있음 → §8 결정 필요**

**공수: 2~3일** (a만) / +1일 (b 선택지에 따라)

### 6.4 [P1] 파기 실효화 — 제21조

**(a) 하객 데이터 파기를 크론으로 이관**
현재 [dashboard/page.tsx:62](app/invitation/%5Bid%5D/dashboard/page.tsx)의 페이지 방문 의존 파기를
`purge-expired-invitations` 크론으로 옮긴다. 페이지의 `after()` 호출은 **안전망으로 남겨둔다**
(크론 장애 시 이중화). 파기 대상에 누락된 `visit_daily_stats`를 추가한다.

**(b) Storage 파일 삭제**
`content_data`·`form_submissions.data`를 순회해 Storage URL을 수집한 뒤 `deleteFile()` 호출.
⚠️ 같은 이미지가 여러 청첩장에 참조될 수 있으므로 **참조 카운트 확인 후 삭제**.

**(c) 소프트 삭제 → 하드 삭제 이관**
`deleted_at`이 찍힌 지 N일(권장 30일, 오삭제 복구 유예) 지난 `customers`·`invitations`를
크론이 실제 DELETE 한다. 유예기간은 처리방침에 명시한다.

**공수: 4~5일**

### 6.5 [P1] IP 해시 강화 — 제29조

```
현재: createHash("sha256").update(ip)              ← 2³² 전수대입으로 복원 가능
개선: createHmac("sha256", SECRET).update(ip + 날짜) ← 키 없이는 복원 불가 + 일 단위 로테이션
```
날짜 솔트를 섞으면 **일별 순 방문자 집계는 유지되면서 장기 추적은 불가능**해진다.
`aggregate-visit-stats`가 이미 일 단위 집계라 통계 기능에 영향이 없다.

**수정:** [app/w/[slug]/page.tsx:174](app/w/%5Bslug%5D/page.tsx) — `DASHBOARD_SESSION_SECRET` 재사용 또는 전용 키

**공수: 0.5일**

### 6.6 [P1] 하객 자기정보 삭제 — 제36조

§4.2 방명록 비밀번호 도입. RSVP는 이미 전화번호 기준 `upsert`로 **수정**은 가능하므로,
"응답 취소" 버튼만 추가하면 삭제권까지 충족된다.

**공수: 2~3일**

### 6.7 [P2] 취급자 접속기록 — 고시 제8조

`audit_logs`를 확장한다(신규 테이블 불필요). 단, 고객 목록 조회처럼 청첩장 단위가 아닌 행위가
있으므로 `invitation_id`를 **nullable로 변경**해야 한다.

기록 대상: 하객 명단 조회, CSV 다운로드, 고객 상세 열람. 보관 1년 이상.

**공수: 3~4일** (조회 지점마다 삽입해야 해서 공수 대비 체감이 낮음 → P2)

---

## 7. 코드로 알 수 없어 확인이 필요한 정보

처리방침 작성 전에 **반드시** 채워야 하는 값들이다. 이게 없으면 §6.1을 시작할 수 없다.

| 항목 | 왜 필요한가 | 비고 |
|---|---|---|
| 사업자명·대표자·사업자등록번호·주소 | 처리방침 필수 기재 | [app/contact/page.tsx:101](app/contact/page.tsx)의 "서울 강남구 테헤란로 123, VOW 빌딩"과 `support@vow.seoul`, `02-123-4567`은 **명백한 플레이스홀더**라 그대로 쓰면 안 됨 |
| 개인정보 보호책임자 (성명·직책·연락처) | 제31조 | 소규모 사업자는 대표자가 겸임 가능 |
| **Supabase 프로젝트 리전** | 국외이전 고지 여부(제28조의8) 결정 | Seoul(ap-northeast-2)이면 국내, 그 외면 국외이전 고지 필요 |
| **Vercel 배포 리전** | 동일 | Vercel Analytics는 미국 처리로 알려져 있어 사실상 국외이전 고지 대상 |
| 신랑신부 정보 보유기간 확정 | 고지문에 명시 | 현재 설정 기본값 30일. 계약·정산 기록은 상법상 별도 보존의무가 있을 수 있음 |

> **국외이전은 2023년 개정으로 "동의" 대신 "처리방침 공개"로 갈음할 수 있다**(제28조의8 제1항 3호).
> 이전항목·국가·수탁자명·목적·보유기간 5가지를 처리방침에 적으면 별도 동의 팝업이 불필요하다.
> → UX 부담이 크게 줄어드는 지점이므로 이 경로를 택한다.

---

## 8. 결정이 필요한 트레이드오프

### 결정 ① `dashboard_password` 해시화 ↔ 관리자 편의

이번 라운드에서 **"고객용 대시보드 복사하기" 토스트에 비밀번호를 함께 표시하는 기능**을 넣었다
(커밋 `84172ce`). 비밀번호를 일방향 해시로 바꾸면 **이 기능이 원리적으로 불가능해진다.**

| 선택지 | 내용 | 영향 |
|---|---|---|
| **A. 해시 + 안내문 대체** (권장) | 저장은 bcrypt. 토스트는 실제 값 대신 "비밀번호는 신랑 연락처 뒷 4자리입니다" 안내 | 고시 완전 준수. 관리자가 값을 못 봄 |
| B. 현행 유지 | 평문 저장 | 안전조치 위반 지속 |
| C. 양방향 암호화 | 관리자만 복호화 | 고시가 요구하는 "일방향"이 아님. 회색지대 |

> A를 권장하는 근거: 이 비밀번호는 애초에 **"연락처 뒷 4자리"라는 규칙으로 생성**되므로
> ([initial_schema.sql:203](supabase/migrations/20260708000000_initial_schema.sql) 주석) 관리자가
> 실제 값을 몰라도 고객에게 안내할 수 있다. 잃는 것이 거의 없다.

### 결정 ② 하객 방문 로그(IP) 수집 자체를 유지할 것인가

동의 없이 수집 중인데, 하객에게 방문 시점에 동의를 받는 것은 UX상 불가능하다(청첩장을 여는 즉시 발생).

| 선택지 | 내용 |
|---|---|
| **A. 처리방침 고지 + 해시 강화로 유지** (권장) | 통계 기능 유지. §6.5로 가역성 제거 |
| B. 수집 중단 | 관리자 통계·신랑신부 대시보드 방문수 기능이 죽음 |

A를 택하되 **§6.5(IP 해시 강화)를 반드시 함께** 해야 한다. 지금처럼 복원 가능한 상태로
"가명처리했다"고 주장하면 오히려 불리하다.

### 결정 ③ 기존 데이터 소급 처리

동의 절차 도입 이전에 수집된 데이터가 이미 DB에 있다. 이론상 동의 없이 수집한 데이터다.

| 선택지 | 내용 |
|---|---|
| **A. 예식 완료분은 보유기간 도래 시 정상 파기** (권장) | 청첩장은 생명주기가 짧아 대부분 곧 파기 대상이 됨 |
| B. 전량 즉시 파기 | 운영 중인 청첩장이 깨짐 |

---

## 9. 로드맵

```
Week 1  ── P0-① 처리방침
           §7 정보 수집 → 본문 작성 → /privacy·/terms → 도달 경로 4곳
           ↳ 선행조건이라 이것부터. 나머지 고지문이 여기서 파생된다

Week 2  ── P0-② 수집 동의
           lib/privacy-consent.ts → 컴포넌트 2종 → 마이그레이션
           → 폼 스텝0 · RSVP · 방명록 · 문의

Week 2  ── P0-③ 비밀번호 (병행 가능, 독립적)
           /api/form-auth + select('*') 제거 → (결정 ① 후) dashboard_password

Week 3~4── P1-④⑤⑥ 파기 실효화 · IP 해시 · 삭제권
           크론 이관 → Storage 삭제 → 하드 삭제 → 방명록 비밀번호

분기 내   ── P2-⑦⑧ 접속기록 · 유출 대응 절차
```

**총 공수: P0 8~11일 / P1 7~9일 / P2 4~6일**

---

## 10. 검증 기준

각 항목은 아래를 통과해야 완료로 본다.

**P0-① 처리방침**
- [ ] `/privacy`가 200으로 응답하고 법정 14개 항목이 모두 존재
- [ ] 청첩장(테마 3종 이상)·폼·대시보드·셀프편집 하단에서 링크 도달
- [ ] 신규 테마를 추가해도 링크가 자동으로 붙음 (테마 HTML 무수정 확인)

**P0-② 동의**
- [ ] 4개 지점 모두 미동의 시 제출 불가
- [ ] 동의 후 저장된 행에 `consent_agreed_at`·`consent_version`이 실제로 기록됨 (라이브 DB 확인)
- [ ] 고지문의 보유기간이 관리자 설정값 변경에 따라 함께 바뀜
- [ ] 동의 상세 텍스트가 모두 12px 이상

**P0-③ 비밀번호**
- [ ] 폼 진입 시 네트워크 응답에 `access_password`가 **포함되지 않음** (DevTools 확인)
- [ ] 잘못된 비밀번호로 `/api/form-auth` 호출 시 401

**P1-④ 파기**
- [ ] 예식일+14일 지난 청첩장을 **아무도 방문하지 않아도** 크론 실행만으로 하객 데이터가 사라짐
- [ ] 파기 후 `rsvp_responses`·`guestbook_entries`·`visit_logs`·`visit_daily_stats` 전부 0건
- [ ] 파기 후 해당 청첩장의 Storage 이미지가 실제로 삭제됨 (타 청첩장 미참조 건에 한해)

**P1-⑤ IP**
- [ ] 같은 IP가 다른 날짜에 다른 해시로 기록됨
- [ ] 일별 순 방문자 집계가 기존과 동일하게 동작

---

## 11. 이 계획에 포함하지 않은 것

| 항목 | 판단 |
|---|---|
| 쿠키 배너 | 이 서비스는 광고·추적 쿠키를 쓰지 않는다. 처리방침 고지로 충분하며, 청첩장에 배너를 띄우면 감성만 해친다 |
| 처리방침 리치텍스트 편집기 | 과잉. 법정 기재사항을 관리자가 실수로 지울 위험이 더 크다 (§4.6) |
| 만 14세 미만 별도 인증 | 청첩장 하객 특성상 저위험. 방명록에 안내 문구만 |
| 정보주체 권리 요구 접수 화면 | 초기에는 처리방침에 명시한 이메일 접수로 충분. 요구 건수가 늘면 재검토 |
| 알림톡 발송 동의 | A2가 §9 보류 상태. 착수 시 **광고성 정보 수신동의(정보통신망법 제50조)** 가 별도로 필요하다는 점만 기록 |
